import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
  container: {
    maxWidth: "75%",
    padding: 10,
    margin: 10,
    borderRadius: 10,
  },

  content: {
    color: "white",
  },
});

export default styles;
